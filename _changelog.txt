				TODO
				----

-faltan por mirar los undefined opcodes ARM

----
|GB|
----

--cronometrar retardo entre VBL y interrupcion de VBL

--DMA NORMAL hace que todo lea FF menos la HRAM

--comprobar tiempos de la gdma en la gameboy real

-RESET -- arreglar en gbc

- memoria encima de la oam es un espejo de la oam?

- que pasa si se usa [rDMA] = FF, o alguna de las direcciones que supuestamente no valen?

-poner wave ram en el visor de IO

-----
|GBA|
-----

-que pasa con los modos 6-7?

-final fantasy tactics totema mateus efecto en LY

-emular cosas raras de la ventana

-- CORREGIR SONIDO!!!!! *************************************

-RTC / REGISTROS DE I/O ????

-SERIAL!!***************************************************

- the correct way of emulating is drawing a pixel every 4 clocks

-TERMINAR DE MIRAR MODO ARM
-ciclos modo arm

-DESACTIVAR CAPAS GBA - configuracion

-emular pipeline gba para que no detecten programas que es un emulador

-arreglar lecturas de memoria de gba, los 2 bits inferiores

---------
|GENERAL|
---------

********************************************************ACTUALIZAR LOS README!!!!!!

-actualizar visores de IO, memoria y tal con f7

-GUARDAR TILESETS, MAPS, MEMORIA...

-EJECUTAR UN FRAME -- menu

-tachar los colores de paleta que no se usan para nada?

-nuevo visor de sprites

-visor de todos los sprites


				CHANGES
				-------
-version 0.2.0
-winapi out, SDL2 in. complete new GUI.
-crea las carpetas screenshots y bios si no existen
-breakpoints
-register values can be changed
-in gba mode, the debugger steps to next instruction executing as many clocks as needed.
 in gb mode not.
-memory viewers improved: memory can be edited, and ascii characters are shown
-drag and drop supported

---GBA EMULATION
-barrel shifter fixes
-undefined instructions in THUMB emulated (except that ones using r15 as operand)
-ldm/stm with writeback with base included in rlist hopefully fixed
-fixed dma sound irq
-SOUNDCNT_H write when sound is off fixed
-special effects (blending, fade white/black) fixed
-GREENSWAP (04000002h) I/O register emulated.
-debugger improved
-debugger shows "true" or "false" to know if the next instruction will be executed (if it is
 conditional)
-debugger shows little arrows in branches to know where they go (up / down)
-debugger shows register name that are accesed by ldr/str instructions
-fixed a bug where some alu instructions in arm mode would be executed with 0 clocks needed.

---GB EMULATION
-DAA fixed
-"ld hl,sp+n" and "add sp,n" flags fixed
-core rewriten
-4x4 World Trophy (Europe) (En,Fr,De,Es,It) WORKS!
-debugger improved
-debugger shows "true" or "false" to know if the next instruction will be executed (if it is
 conditional)
-debugger shows little arrows in branches to know where they go (up / down)
-debugger shows register name that are accesed by instructions



					KNOWN BUGS
					----------

-sound glitches? -- buffer underflow?
-Affine sprites/bgs(mode 7) + mosaic = bad
-reg_write_8 WILL ENTER HALT MODE WHEN WRITING TO REG_POSTFLG
-gba cpu timings bad


					TODO
					----

-THUMB LDMIA/STMIA: Strange Effects on Invalid Rlist's
Empty Rlist: R15 loaded/stored, and Rb=Rb+40h.
Writeback with Rb included in Rlist: Store OLD base if Rb is FIRST entry in
Rlist, otherwise store NEW base,no writeback.
-terminar ultima pag del visor IO
-goto en el desensamblador
-mejorar debugger -- cambiar memoria, ir a una direccion de memoria en desensamblador
-hacer que se detecte el tipo de rom a partir de los headers
-gba rumble
-gb rumble
-gb camera
-custom controls ************
-arreglar debugger -> quitar los static de los checkboxes y poner el texto al boton
-joypad al debugger -- IOviewer?
-arreglar swi 11 y 12 (offset) ?
-arreglar timings cpu gba
-Caution: A very large OBJ (of 128 pixels vertically, ie. a 64 pixels OBJ in
a Double Size area) located at Y>128 will be treated as at Y>-128, the OBJ is
then displayed parts offscreen at the TOP of the display, it is then NOT
displayed at the bottom.
-Video Capture Mode (DMA3 only)
Intended to copy a bitmap from memory (or from external hardware/camera) to
VRAM. When using this transfer mode, set the repeat bit, and write the number
of data units (per scanline) to the word count register. Capture works similar
like HBlank DMA, however, the transfer is started when VCOUNT=2, it is then
repeated each scanline, and it gets stopped when VCOUNT=162.
-ATTRACTION MODE (SGB) NO SERA LO DE QUE SE MUEVA EL BIDUJITO EN FONDOS DE SISTEMA?

//---------------------------------------------------------------------

Reading a 32 bit value in ROM from EWRAM: 10 cycles
wait, wait, read instruction, compute address, wait, wait, wait, read low bits, wait, read high bits

Reading a 32 bit value in EWRAM from EWRAM: 10 cycles
wait, wait, read instruction, compute address, wait, wait, read low bits, wait, wait, read high bits

Loading a 16 bit value using mov/lsl/mov/orr: 12 cycles
(wait, wait, read instruction) * 3


